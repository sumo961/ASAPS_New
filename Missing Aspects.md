Issues needing attention


- The save button never appears even if there are pending changes
- Are the - The "Save visual changes button" and the "save changes" in the inspector duplicate functionality and are confusing. Instead have one automatic save function and a save button that the user can press. 
- preferences are still not fully synced, e.g. different available fonts, text box appearances are ignored
- Text should be in the middle of text boxes, but is below the middle
- Text animations should be one text box and then the next and then buttons 

- 

- Default settings in preferences should be overhauled to look more pleasing (e.g. white text boxes, ariel font, blue buttons with white font)
- Preferences need to be fully synced with visual editor and preview
- exported assets are not imported again when story is imported again
- character graphics are exported in the "props" folder
- "new story" button is missing
- Save feature is missing
- undo is missing
- Save/backup feature: backup should save all aspects of a work in progress, while export should only export what's needed for playback, e.g. only graphics which are referenced in the realized story structure. Both could use the same ASML structure, but with additional data for aspects such as unused assets, beat positions in the flowchart etc. 


  UI Enhancements (1-2 hours):
  - Upload size warnings in AssetManager component UI
  - Storage capacity indicator/progress bar
  - Visual feedback for quota warnings
